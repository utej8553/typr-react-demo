apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: demo
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 3000
      nodePort: 30080
      targetPort: 3000
